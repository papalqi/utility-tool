# 贡献指南

感谢你对 PC Utility Tool 的关注！我们欢迎各种形式的贡献。

## 🤝 如何贡献

### 报告问题

如果你发现了 bug 或有功能建议：

1. 在 [Issues](https://github.com/papalqi/pc-utility-tool-electron/issues) 中搜索是否已有相关问题
2. 如果没有，创建一个新的 Issue，并提供：
   - 清晰的标题和描述
   - 复现步骤（如果是 bug）
   - 期望的行为
   - 实际的行为
   - 你的环境信息（操作系统、Node.js 版本等）
   - 相关的日志或截图

### 提交代码

1. **Fork 本仓库**

2. **克隆到本地**
   ```bash
   git clone https://github.com/你的用户名/pc-utility-tool-electron.git
   cd pc-utility-tool-electron
   ```

3. **安装依赖**
   ```bash
   # 使用项目提供的安装脚本
   ./install.ps1   # Windows
   ./install.sh    # Linux/macOS
   ```

4. **创建功能分支**
   ```bash
   git checkout -b feature/your-feature-name
   ```

5. **开发你的功能**
   - 遵循项目的代码规范（见下文）
   - 添加必要的注释
   - 确保代码可以通过 lint 和类型检查

6. **测试你的更改**
   ```bash
   npm run type-check  # 类型检查
   npm run lint        # 代码检查
   npm run dev         # 本地运行测试
   ```

7. **提交更改**
   ```bash
   git add .
   git commit -m "feat: 添加新功能的简短描述"
   ```
   
   提交信息格式：
   - `feat:` 新功能
   - `fix:` Bug 修复
   - `docs:` 文档更新
   - `style:` 代码格式（不影响功能）
   - `refactor:` 代码重构
   - `test:` 测试相关
   - `chore:` 构建或辅助工具

8. **推送到你的 Fork**
   ```bash
   git push origin feature/your-feature-name
   ```

9. **创建 Pull Request**
   - 在 GitHub 上创建 PR
   - 描述你的更改和原因
   - 引用相关的 Issue（如果有）
   - 等待维护者审核

## 📝 代码规范

### TypeScript 风格

- 使用 **TypeScript** 编写所有代码
- 使用函数式组件和 Hooks（React）
- 缩进：**2 个空格**
- 不使用分号
- 命名：
  - 变量/函数：`camelCase`
  - 组件：`PascalCase`
  - 常量：`UPPER_SNAKE_CASE`
  - 文件名：`kebab-case` 或 `PascalCase`（组件）

### 代码质量

- 运行 `npm run type-check` 确保无类型错误
- 运行 `npm run lint` 确保符合 ESLint 规则
- 可选：运行 `npm run format` 自动格式化代码

### 日志规范

项目由 AI 驱动开发，良好的日志至关重要：

```typescript
// 使用 widgetLogger 记录关键操作
widgetLogger.info('操作成功', { data })
widgetLogger.error('操作失败', error)
widgetLogger.warn('警告信息')
```

### Widget 开发规范

如果你要开发新的 Widget：

1. 使用 Hook 堆栈：
   - `useWidget` - 基础功能
   - `useWidgetActions` - 操作管理
   - `useWidgetStorage` - 本地存储
   - `useWidgetObsidian` - Obsidian 集成

2. 使用 `WidgetLayout` 包裹
3. 声明 `WidgetMetadata`
4. 添加完整的类型定义

示例见 `src/widgets/` 目录下的现有 Widget。

## 🧪 测试

目前项目主要依赖手动测试：

- 在开发模式下充分测试你的更改
- 测试 Obsidian 启用和禁用两种情况（如果相关）
- 确保不破坏现有功能

我们欢迎添加自动化测试！

## 📚 文档

- 重大功能更改需要更新 README.md
- 架构相关的文档在 `docs/` 目录
- 代码注释使用中文，方便国内开发者理解
- 文档和注释要清晰、准确

## 🐛 调试技巧

### 查看日志

开发模式：
```bash
npm run dev
# 打开 Chrome DevTools (F12) 查看渲染进程日志
# 查看终端输出了解主进程日志
```

生产模式日志采集：
```bash
# 使用日志采集脚本
./scripts/dev/dev-loop-with-logs.ps1  # Windows
./scripts/dev/dev-loop-with-logs.sh   # Linux/macOS
# 日志保存在 logs/ 目录
```

### 环境问题

如果遇到环境相关问题：
```bash
pwsh -ExecutionPolicy Bypass -File .\scripts\tools\env-diagnostics.ps1
```

## ❓ 需要帮助？

- 查看 [README.md](./README.md) 了解项目基本信息
- 查看 [docs/](./docs/) 目录下的文档
- 在 [Issues](https://github.com/papalqi/pc-utility-tool-electron/issues) 中提问

## 📜 许可证

通过贡献代码，你同意你的贡献将在 MIT 许可证下发布。

---

再次感谢你的贡献！🎉
