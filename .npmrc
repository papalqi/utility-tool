# Project-level npm configuration

# Ensure consistent dependencies across machines
save-exact=false
engine-strict=false
package-lock=true

# Use official npm registry (avoid mirror differences)
# registry=https://registry.npmjs.org/

# Prevent optional dependencies issues
optional=true

